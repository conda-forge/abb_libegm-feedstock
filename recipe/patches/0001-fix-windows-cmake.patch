diff --git a/CMakeLists.txt b/CMakeLists.txt
index 1f1537e..3b0ed30 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -18,24 +18,12 @@ if(WIN32)
 endif()
 
-#########################
-## Boost C++ Libraries ##
-#########################
-find_package(Boost REQUIRED COMPONENTS regex system thread)
-
-#############################
-## Google Protocol Buffers ##
-#############################
-
-# Temporary workaround for https://github.com/ms-iot/ROSOnWindows/issues/218
-if(WIN32)
-  foreach(prefix IN ITEMS $ENV{CMAKE_PREFIX_PATH})
-    if(${prefix} STREQUAL "C:/opt/rosdeps/x64")
-      list(APPEND CMAKE_PROGRAM_PATH "C:/opt/rosdeps/x64/tools/protobuf")
-    endif()
-  endforeach()
-endif()
+find_package(Boost REQUIRED COMPONENTS regex thread)
 
-find_package(Protobuf REQUIRED)
+if(POLICY CMP0077)
+  cmake_policy(SET CMP0077 NEW)
+endif()
+set(protobuf_MODULE_COMPATIBLE ON)
+find_package(Protobuf CONFIG REQUIRED)
 
 # Make sure protoc is present, as apparently the above find_package() doesn't check that.
 if(NOT PROTOBUF_PROTOC_EXECUTABLE)
@@ -90,17 +75,17 @@ add_library(${PROJECT_NAME}::${PROJECT_NAME} ALIAS ${PROJECT_NAME})
 include(GenerateExportHeader)
 generate_export_header(${PROJECT_NAME})
 
+target_compile_features(${PROJECT_NAME} PUBLIC cxx_std_17)
+
 target_include_directories(${PROJECT_NAME} PUBLIC
   "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include;${CMAKE_CURRENT_BINARY_DIR}>"
   $<INSTALL_INTERFACE:$<INSTALL_PREFIX>/include>
-  ${PROTOBUF_INCLUDE_DIRS}
 )
 
 target_link_libraries(${PROJECT_NAME} PUBLIC
   Boost::regex
-  Boost::system
   Boost::thread
-  ${PROTOBUF_LIBRARIES}
+  protobuf::libprotobuf
   Threads::Threads
 )
 
@@ -109,8 +94,18 @@ if(NOT BUILD_SHARED_LIBS)
 endif()
 
 if(MSVC)
-  # Force include the export header when using Microsoft Visual C++ compiler.
-  target_compile_options(${PROJECT_NAME} PUBLIC "/FI${CMAKE_CURRENT_BINARY_DIR}/${PROJECT_NAME}_export.h")
+  target_compile_options(${PROJECT_NAME} PUBLIC
+    "$<BUILD_INTERFACE:/FI${CMAKE_CURRENT_BINARY_DIR}/${PROJECT_NAME}_export.h>"
+    "$<INSTALL_INTERFACE:/FI${PROJECT_NAME}/${PROJECT_NAME}_export.h>"
+    "/wd4251"
+    "/wd4275"
+  )
+
+  target_compile_definitions(${PROJECT_NAME} PUBLIC
+    _WIN32_WINNT=0x0601
+    PROTOBUF_USE_DLLS
+    ABSL_CONSUME_DLL
+  )
 endif()
 
 #############
diff --git a/cmake/abb_libegmConfig.cmake.in b/cmake/abb_libegmConfig.cmake.in
index fdd51ab..21882bb 100644
--- a/cmake/abb_libegmConfig.cmake.in
+++ b/cmake/abb_libegmConfig.cmake.in
@@ -6,7 +6,8 @@ include(CMakeFindDependencyMacro)
 
 # Find dependencies
 find_dependency(Threads REQUIRED)
-find_dependency(Boost REQUIRED COMPONENTS regex system thread)
+find_dependency(Boost REQUIRED COMPONENTS regex thread)
+find_dependency(Protobuf CONFIG REQUIRED)
 
 # Our library dependencies (contains definitions for IMPORTED targets)
 include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@Targets.cmake")
